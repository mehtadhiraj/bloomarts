{%- comment -%}
  Diagnostic probe. Deleted once the fault is identified.
{%- endcomment -%}

<section class="probe-collection"><p>probe-collection rendered</p>
  <span>{{ section.settings.collection }}</span>
</section>

{% schema %}
{
  "name": "Probe collection",
  "settings": [
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    }
  ]
}
{% endschema %}
