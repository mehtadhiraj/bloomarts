<div class="section page-width page-width--narrow">
  <h1 class="page__title">{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</div>

<style>
  .page__title {
    margin-bottom: var(--space-md);
    font-size: var(--text-2xl);
  }
</style>

{% schema %}
{
  "name": "Page",
  "settings": []
}
{% endschema %}
